package com.pharmaflow.smartfeatures.enums.recommendation;

/** Interaction events that can be recorded for a recommendation. */
public enum RecommendationEventType {
  VIEWED,
  CLICKED,
  ADDED_TO_CART,
  PURCHASED,
  DISMISSED,
  RESERVATION_REQUESTED,
  RESERVATION_CONFIRMED,
  RESERVATION_FAILED,
  RESERVATION_COMPENSATED
}
